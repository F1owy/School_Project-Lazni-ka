@model DataLayer.entities.ProductEntity
@{
    ViewData["Title"] = Model.Name;
}

<h2>@Model.Name</h2>

<p><strong>KategÛria:</strong> @Model.Category</p>
<p><strong>Cena:</strong> @Model.Price.ToString("N2") Ä</p>
<p><strong>Popis:</strong> @Model.Description</p>
<p><strong>Na sklade:</strong> @Model.StockAmount ks</p>

<form method="post" asp-controller="Cart" asp-action="AddItem">
    <input type="hidden" name="productId" value="@Model.id" />
    <label for="quantity">Mnoûstvo:</label>
    <input type="number" id="quantity" name="quantity" value="1" min="1" max="@Model.StockAmount" />
    <button type="submit">Do koöÌka</button>
</form>

<p><a href="/Products">? Sp‰ù na produkty</a></p>
